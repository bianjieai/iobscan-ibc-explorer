export type TBaseDenoms = {
  chain_id: string
  create_at: number
  denom: string
  icon: string
  is_main_token: boolean
  scale: number
  symbol: string
  update_at: number
}

export type TBaseChains = {
  chain_id: string
  chain_name: string
  icon: string
}