export enum IBC_TX_STATUS {
    'default' = 1000, // web custom staus
    'success' = 1,
    'failed' = 2,
    'processing' = 3,
    'refund' = 4,
    'setting' = 5
}

export enum IBC_SC_AND_DC_TX_STATUS {
    'default' = 1000, // web custom staus
    'success' = 1,
    'failed' = 0
}
